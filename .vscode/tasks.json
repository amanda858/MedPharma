{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "git-commit-push",
            "type": "shell",
            "command": "git add -A && git commit -m 'fix: comprehensive permanent bug fixes — SQL injection, connection leaks, session security, claims upsert, SLA keys, status normalization, and 20+ more' && git push origin main",
            "options": {
                "cwd": "/workspaces/CVOPro"
            },
            "problemMatcher": []
        }
    ]
}